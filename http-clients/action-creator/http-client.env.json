{
  "local": {
    "api_url": "http://127.0.0.1:8000/api/v1.0",
    "Security": {
      "Auth": {
        "auth-id": {
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{client_id}}",
          "Username": "{{username}}",
          "Password": "{{password}}",
          "Auth URL": "{{auth_url}}",
          "Token URL": "{{token_url}}",
          "Scope": "openid"
        }
      }
    }
  },
  "dev": {
    "api_url": "https://dev.lot2.eodatahub.org.uk/api/v1.0",
    "Security": {
      "Auth": {
        "auth-id": {
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{client_id}}",
          "Username": "{{username}}",
          "Password": "{{password}}",
          "Auth URL": "{{auth_url}}",
          "Token URL": "{{token_url}}",
          "Scope": "openid"
        }
      }
    }
  },
  "test": {
    "api_url": "https://test.lot2.eodatahub.org.uk/api/v1.0",
    "Security": {
      "Auth": {
        "auth-id": {
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{client_id}}",
          "Username": "{{username}}",
          "Password": "{{password}}",
          "Auth URL": "{{auth_url}}",
          "Token URL": "{{token_url}}",
          "Scope": "openid"
        }
      }
    }
  },
  "staging": {
    "api_url": "https://staging.lot2.eodatahub.org.uk/api/v1.0",
    "Security": {
      "Auth": {
        "auth-id": {
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{client_id}}",
          "Username": "{{username}}",
          "Password": "{{password}}",
          "Auth URL": "{{auth_url}}",
          "Token URL": "{{token_url}}",
          "Scope": "openid"
        }
      }
    }
  }
}
